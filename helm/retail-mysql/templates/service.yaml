apiVersion: v1
kind: Service
metadata:
  name: retail-mysql
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: retail-mysql
  ports:
    - port: 3306
      targetPort: 3306
  clusterIP: None   # Headless pour stabilité DNS
