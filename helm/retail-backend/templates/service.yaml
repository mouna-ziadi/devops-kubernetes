apiVersion: v1
kind: Service
metadata:
  name: retail-backend
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: retail-backend
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
